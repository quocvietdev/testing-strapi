'use strict';

/**
 * config-web-ordering router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::config-web-ordering.config-web-ordering');