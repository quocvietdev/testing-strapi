'use strict';

/**
 * config-reward-banner service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::config-reward-banner.config-reward-banner');