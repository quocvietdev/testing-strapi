'use strict';

/**
 * config-reward-banner router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::config-reward-banner.config-reward-banner');