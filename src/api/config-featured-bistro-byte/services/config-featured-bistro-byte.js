'use strict';

/**
 * config-featured-bistro-byte service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::config-featured-bistro-byte.config-featured-bistro-byte');