'use strict';

/**
 * config-featured-bistro-byte controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::config-featured-bistro-byte.config-featured-bistro-byte');