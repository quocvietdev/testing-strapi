'use strict';

/**
 * config-featured-bistro-byte router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::config-featured-bistro-byte.config-featured-bistro-byte');